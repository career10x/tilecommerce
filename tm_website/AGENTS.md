<!-- BEGIN:agent-rules -->

## Project Instructions

You are helping build a Next.js E-Commerce website. 

### Tech Stack
- Next.js 16 (App Router)
- React 19
- TypeScript
- Tailwind CSS
- Shadcn UI

### Coding Conventions
1. Use functional components with React Hooks.
2. Enforce TypeScript strictness.
3. Use `async/await` for data fetching.
4. Code must be ESLint and Prettier compliant.
5. Always write descriptive comments for complex logic.

### Development Workflow
1. Check `AGENTS.md` for project-wide rules.
2. Read component code before modifying.
3. Test changes locally before proposing PRs.
4. Do not break backward compatibility unless explicitly allowed.

### Commit Messages
- Format: `<type>: <short summary>`
- Examples: `feat: add user authentication`, `fix: resolve navigation bug`
- Use Conventional Commits specification.

### Testing
- Run `npm run dev` to test locally.
- Ensure Tailwind styles are correctly applied.
- Verify components are responsive across devices.

### Deployment
- Preview changes in the dev environment.
- Do not push untested code to main branch.

## Environment Rules

You are working on the `d:\Learning\tilemart_new_19_05_2026\tm_website` directory.

1. File paths must be relative to the project root.
2. When referring to installed packages, use `npm install` commands.
3. Do not modify configuration files unless necessary, and always back them up first.
4. When writing code, always use the TypeScript syntax.
5. When writing HTML, always use Tailwind CSS for styling and responsiveness.

## Output Rules

1. Provide clear, concise summaries of changes.
2. Include code examples in markdown code blocks.
3. For UI changes, include before-and-after descriptions.
4. Explain complex logic in comments within the code.
5. Format all technical information using Markdown.

## Error Handling

1. Check for TypeScript type errors before suggesting code.
2. Verify Tailwind class names against the Tailwind configuration.
3. Ensure Next.js App Router conventions are followed.
4. Test for broken links or components before finalizing changes.

<!-- END:agent-rules -->
